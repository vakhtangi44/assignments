function notContains(array) {
    let min = 0;
    let newMin = 1
    while (min !== newMin) {
        min = newMin;
        for (let i = 0; i < array.length; i++) {
            if (newMin === array[i]) newMin++;
        }
    }
    return newMin;
}
